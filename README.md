# makara AFL++ package

AFL++ packaged for easy pip installation.

## Installation

First install the prerequisite packages:

```bash
chmod +x init.sh
./init.sh
```

Then install `makara-afl`:

```bash
pip3 install git+https://github.com/ZERO-A-ONE/makara-afl.git
```
